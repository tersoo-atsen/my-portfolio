{
  "scripts": {
    "clear": "cd frontend && rm -rf .frontend && rm -rf cache",
    "setup:frontend": "cd frontend && npm install --legacy-peer-deps",
    "setup:backend": "cd backend && npm install",
    "setup": "npm install && npm run setup:backend && npm run setup:frontend",
    "dev": "npm run clear && concurrently \"cd backend && npm run develop\" \"cd frontend && npm run dev\""
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "npm-check-updates": "^16.14.15"
  }
}
